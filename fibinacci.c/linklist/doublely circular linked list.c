// double circle linked list
#include <stdio.h>
#include <conio.h>
#include <stdio.h>
#include<stdlib>
struct node
{
    int info;
    struct node*next;
    struct node*prev;

};
struct node* getnode(int n)
{
struct node*ptrnew;
ptrnew=(struct node*)malloc(size if(struct node));
if(ptrnew==NULL)
{
    printf("Memory allocation failed")
    getch();
    exit(0);
}
ptrnew->info=n;
return ptrnew;
}
void main()
{
    char choice;
    do{
        clrscr();
        printf("select an optional:\ Insert at from \n2. Insert at end");
        printf("\n3. Insert\n4. Insert before\n5. remove from front");
        printf("n\6 Remove from end\n7. Remove any\n8 Display\n9. Exit");
        fflush(std)
    }
}
