#include <stdio.h>
#include <conio.h>
#define Max 10
void main()
{
    int arr[]={7,5,3,11,9,15,2,7,3,7}
    int i,n=sizeof(arr)/sizeof(int);
    clrscr();
    printf("Before sorting....\n");
    for("i=0; i<n; i++");
    printf("i=0;i<n;i++");
    printf("%d\t",arr[i]);
    countsort(arr,n);
    printf("sorting....");
    for(i=0;i<n;i++);
    printf("")

}
void countsort(int arr[],int n)
{
    int count[maxvalue]={0};
    int temp[SIZE];
    int k=getHighestNumber(arr,n);
    int i;
    for(i=0;i<i++)
    count[arr[i]]++;

}